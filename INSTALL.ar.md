# تثبيت وكيل Rakz

طريقتان توصلان لنفس النتيجة. **المسار أ** هو "نقرة الزر": بتعطي أداة الـ AI
تبعتك برومبت واحد وهي بتثبّت كل شي عنك. **المسار ب** هو نفس الشي بإيدك.

بالحالتين، اللي "بينثبّت" هو: أربع مجلدات مهارات (نصوص)، ملف وكيل اختياري،
أداة سطر أوامر صغيرة (`mns`، بدها Node.js)، ومجلد ذاكرة محلي. ما في شي
بيشتغل بالخلفية.

> **المصدر:** من وين ما جبت هالملفات — مستودع مستنسخ، مجلد منزّل، أو
> `<REPO-URL>` بعد النشر. تحت، كلمة `SOURCE/` يعني هذا المجلد.

---

## المسار أ — نقرة زر (بمساعدة AI)

انسخ البلوك الموجود في [INSTALL.md](INSTALL.md) تحت عنوان
**"Path A — one click"** كاملاً والصقه في أداة الـ AI تبعتك
(Claude Code أو أي أداة بتقدر تقرأ وتكتب ملفات). البرومبت نفسه بالإنجليزي
لأنه موجّه للأداة مش إلك — وآخر خطوة فيه إنها ترد عليك **بلغتك أنت** وتعرض
تبدأ أول جلسة استذكار.

باختصار، البرومبت بيخلّي الأداة:

1. تكتشف نوع الأداة (harness) ووين بتحمّل المهارات منه.
2. تنسخ المهارات الأربع (`rakz-mns-format`، `rakz-study-loop`،
   `rakz-library-loop`، `rakz-agent-memory`) لمجلد المهارات.
3. تثبّت تعريف الوكيل `rakz-tutor` إذا الأداة بتدعم وكلاء مخصصين.
4. إذا الأداة ما بتدعم مهارات أصلاً — بتلجأ لخطة بديلة عبر `AGENTS.md`.
5. تنسخ أداة `mns` إلى `~/.rakz/agent/tools/` وتعمللها لانشر (اختصار) عشان
   تشتغل من أي مكان — وإذا Node.js مش منصّب بتتخطاها وبتخبرك.
6. تنشئ مجلد الذاكرة `~/.rakz/memory/` إذا مش موجود.
7. تتحقق إنه كل شي مركّب صح، وبعدها ترحّب فيك وتعرض تبلّش.

---

## المسار ب — يدوي

1. **المهارات.** انسخ المجلدات الأربعة من `SOURCE/skills/` إلى مجلد المهارات
   تبع أداتك:
   - Claude Code (شخصي، لكل المشاريع): `~/.claude/skills/`
   - Claude Code (لمشروع واحد): `<project>/.claude/skills/`
   - أدوات أخرى: حسب توثيقها لصيغة Agent Skills / ملفات `SKILL.md`.

   النتيجة مثلاً: `~/.claude/skills/rakz-mns-format/SKILL.md` (ومثلها للباقي).

2. **الوكيل (اختياري).** إذا أداتك بتدعم وكلاء مخصصين، انسخ
   `SOURCE/agents/rakz-tutor.md` لمجلد الوكلاء (Claude Code: `~/.claude/agents/`).

3. **الأداة.** انسخ `SOURCE/tools/` إلى `~/.rakz/agent/tools/`، واختيارياً
   اعمل لانشر `~/.local/bin/mns` (شوف النص الجاهز في [INSTALL.md](INSTALL.md)).
   بدها Node.js — إذا مش منصّب تخطاها، المهارات بتقرأ الملفات مباشرة.

4. **الذاكرة.** أنشئ مجلد `~/.rakz/memory/` وفيه ثلاث ملفات — `style.mns`
   و`persona.mns` و`journal.mns` — كل واحد فيه سطر واحد فقط: `%RAKZ 0.1`.

5. **التحقق.** افتح جلسة واطلب من الـ AI: "ابدأ لوب استذكار Rakz".
   المفروض يلقط المهارات ويبدأ من نواة ذاكرته.

---

## الخطة البديلة (أداة بدون دعم مهارات)

انسخ `SOURCE/skills/` إلى `~/.rakz/agent/skills/`، وأضف "bootstrap block"
الموجود في [INSTALL.md](INSTALL.md) لملف التعليمات اللي أداتك بتحمّله دايماً
(مثل `AGENTS.md`).

---

## إزالة التثبيت

احذف مجلدات المهارات الأربعة `rakz-*`، وملف `rakz-tutor.md`، وإذا بدك بياناتك
وبروفايلك كمان — مجلد `~/.rakz/`. هذا كل شي.
